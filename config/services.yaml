parameters:

services:
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'

    # Use This syntax to override a service class
    #ProgramCms\UserBundle\Service\HelloService:
        #class: App\HelloService
