<ul class="navbar-nav mb-2 mb-lg-0">
    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
               aria-haspopup="true" aria-expanded="false">
                Account
            </a>
            <div class="dropdown-menu dropdown-menu-end">
                <a class="dropdown-item" href="#">Profile</a>
                <a class="dropdown-item" href="#">My Topics</a>
                <a class="dropdown-item" href="#">My Replies</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('frontend_user_edit_index') }}">Edit Profile</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('frontend_user_account_logout') }}">Logout</a>
            </div>
        </li>
    {% endif %}
</ul>