{#
/*
 * Copyright © ProgramCMS. All rights reserved.
 * See COPYING.txt for license details.
 *
 * Developed by Mohamed EL QUCHIRI <elquchiri@gmail.com>
 */
#}
<div class="collapser">
    {% if block.hasLabel() %}
        <a class="collapser-label open" data-bs-toggle="collapse" href="#{{ block.getName() }}" role="button"
           aria-expanded="false" aria-controls="{{ block.getName() }}">
            {{ block.getLabel() }}
        </a>
    {% endif %}
    <div class="collapse {% if block.isOpen() %}show{% endif %}" id="{{ block.getName() }}">
        {% if block.hasLabel() %}<div class="collapser-container">{% endif %}
            {{ block.getChildHtml()|raw }}
            {% if block.hasLabel() %}</div>{% endif %}
    </div>
</div>